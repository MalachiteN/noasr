# noasr

Voice input method using Xiaomi MiMo Omni multimodal model - no ASR preprocessing, just raw audio + LLM.

## Quick Start

```bash
pip install noasr
noasr
```

## Development

```bash
pip install -e ".[dev]"
pytest
```
